# autom8 — QA Status

> Auto-generated by `node scripts/qa-report.mjs` on 2026-06-23 20:41 UTC.
> Edit the manifest in that script, not this file (it's overwritten).

**Result:** 🟢 PASS — 72/72 tests passing across 12 files.

Scope: every pure / isolatable function (business rules, parsers, formatters, auth logic). Functions that need a live external service are listed in §2 — they're verified by integration tests / manual QA, not here. Per your ask, the GitHub-dependent functions are explicitly out of scope.

## 1. Verified clean (unit-tested)

| Status | Area | Module | Functions |
|:--:|---|---|---|
| ✅ 19 passing | Access control (visibility + permissions) | `src/lib/access.ts` | `visibleProjects`, `visibleTasks`, `canCreateProject`, `canManageProject`, `canCreateTaskInProject`, `creatableProjects`, `canEditTask`, `canToggleTask` |
| ✅ 6 passing | Server API authorization (token verify + roles) | `src/lib/route-auth.ts` | `authorize — degradation, 401, 403, role-gate, success` |
| ✅ 5 passing | CLI bearer-token auth | `src/lib/api-auth.ts` | `isAuthorized` |
| ✅ 4 passing | Rate limiting (paid-endpoint abuse guard) | `src/lib/rate-limit.ts` | `checkRateLimit`, `sweepRateLimits` |
| ✅ 12 passing | GitHub payload parsing + HMAC (no network) | `src/lib/github.ts` | `generateWebhookSecret`, `verifyGithubSignature`, `parsePushCommits`, `repoFullNameFromPayload` |
| ✅ 3 passing | Daily report formatting | `src/lib/discord.ts` | `buildDailyEmbed (+ bullets/truncation)` |
| ✅ 4 passing | Document-kind detection | `src/lib/doc-parse.ts` | `detectDocKind`, `mimeForKind` |
| ✅ 4 passing | Role-aware navigation + seed integrity | `src/lib/dashboard-data.ts` | `navGroupsFor`, `seed referential integrity` |
| ✅ 8 passing | Central env-var validation | `src/lib/env.ts` | `checkEnv (required/optional/oneOf)`, `formatEnvReport` |
| ✅ 6 passing | Atomic AUT-N key arithmetic | `src/lib/task-keys.ts` | `computeTaskKeys (monotonic, floor-repair, batch)` |

## 2. Not covered here — needs a live external service

These are *not broken* — they just can't be proven correct without the real service (or an emulator/mock). Status = ⏸ pending integration test.

### ⏸ GitHub (network / App install)
_Why excluded: calls the GitHub API or receives its webhooks._

- `src/lib/github-app.ts → createIssue, listAccessibleRepos (parseIssuesPayload is pure and could be extracted)`
- `src/app/api/github/issues/route.ts, github/repos/route.ts`
- `src/app/api/github/app/route.ts, github/webhook/[id]/route.ts`

### ⏸ Firebase / Firestore
_Why excluded: reads/writes a live database — now partly covered by the Firestore emulator suite (tests/integration/firestore.itest.ts → `pnpm test:integration`): document round-trip, daily-report dedupe, and concurrent AUT-N allocation. The rest still need emulator/manual QA._

- `src/lib/firestore.ts, firestore-admin.ts (all CRUD, seeding, propagateAssigneeRename)`
- `src/lib/firebase.ts, firebase-admin.ts, storage.ts (SDK init / uploads)`
- `src/app/api/tasks/route.ts, tasks/update/route.ts, auth/provision/route.ts`
- `src/app/api/projects/[id]/document|webhooks|commits routes`

### ⏸ Anthropic (Claude)
_Why excluded: a paid AI request._

- `src/lib/ai.ts → generateTasksFromDocument`
- `src/app/api/projects/[id]/generate-tasks/route.ts`

### ⏸ Discord (network)
_Why excluded: posts to a live webhook._

- `src/lib/discord.ts → sendDiscordEmbed`
- `src/app/api/reports/daily/route.ts (send path; grouping logic is unit-testable next)`

### ⏸ Browser / React UI
_Why excluded: components need a DOM/component test runner (jsdom + Testing Library)._

- `src/components/dashboard/* (provider, auth-provider, sign-in, dialogs, sidebar, …)`

## 3. Per-file results

| Test file | Passed | Failed |
|---|--:|--:|
| `tests/access.test.ts` | 19 | 0 |
| `tests/api-auth.test.ts` | 5 | 0 |
| `tests/dashboard-data.test.ts` | 4 | 0 |
| `tests/discord.test.ts` | 3 | 0 |
| `tests/doc-parse.test.ts` | 4 | 0 |
| `tests/env.test.ts` | 8 | 0 |
| `tests/github-parse.test.ts` | 12 | 0 |
| `tests/rate-limit.test.ts` | 4 | 0 |
| `tests/route-auth.enforced.test.ts` | 5 | 0 |
| `tests/route-auth.test.ts` | 1 | 0 |
| `tests/smoke.test.ts` | 1 | 0 |
| `tests/task-keys.test.ts` | 6 | 0 |

## 4. How to run

```bash
pnpm qa              # run the suite + regenerate this file (node scripts/qa-report.mjs)
pnpm test            # just the unit tests (Vitest)
pnpm test:watch      # watch mode while developing
pnpm emulators       # start the Firebase emulators (needs the Firebase CLI + Java)
pnpm test:integration  # emulator-backed integration tests (self-skip if no emulator)
```

> esbuild's build is pre-approved in `pnpm-workspace.yaml` (`allowBuilds`), so pnpm scripts run without prompts.

Next QA expansion: extend the emulator suite (tests/integration) to the remaining CRUD + the security rules (auth contexts), and add a jsdom + Testing Library runner for the React components.
